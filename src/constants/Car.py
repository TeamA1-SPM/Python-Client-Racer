# Klasse zum erstellen eines Autos
class Car:
    def __init__(self, name, offset, position, speed, width):
        self.name = name
        self.offset = offset
        self.position = position
        self.speed = speed
        self.width = width